# Local Hoot-Compatible Packs

Place local test packs here when running regression tests. Do not commit game
or music data unless it is explicitly redistributable.

Recommended metadata to commit instead:

- Expected filenames.
- SHA-256 hashes in `*.sha256` files.
- Notes about which legally obtained release produced the files.
